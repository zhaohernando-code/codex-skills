# Codex Custom Skills Bridge

This local Claude Code plugin exposes personal Codex skills from:

```text
~/.codex/skills
```

The `skills/` entries are symlinks back to Codex skill directories. Re-run:

```bash
~/.claude/plugins/local/codex-custom-skills/scripts/sync-codex-skills.sh
```

after adding or removing Codex skills.
