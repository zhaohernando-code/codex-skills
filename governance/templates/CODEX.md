# Governance Workspace Rules

- `CODEX_WORKFLOW_ROOT` points at the local workflow governance root.
- Hook manifests are the source of truth for generated agent hooks.
- Runtime state belongs in local ignored state directories, never in this public governance kit.
